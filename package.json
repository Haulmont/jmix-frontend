{
  "name": "root",
  "private": true,
  "engines": {
    "node": ">=12",
    "npm": ">=6"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.1",
    "@commitlint/config-conventional": "^12.1.1",
    "@commitlint/travis-cli": "^12.1.1",
    "commitizen": "^4.2.3",
    "cz-lerna-changelog": "^2.0.3",
    "eslint": "^7.32.0",
    "prettier": "^2.4.1",
    "husky": "^4.0.6",
    "lerna": "^3.19.0",
    "mkdirp": "^1.0.4",
    "rimraf": "^2.7.1",
    "semantic-release": "^17.4.2"
  },
  "scripts": {
    "lerna": "lerna",
    "lerna-bootstrap": "npm run lerna bootstrap --no-audit",
    "commit": "node node_modules/commitizen/bin/git-cz",
    "bootstrap-react-app": "node scripts/bootstrap-react-app.js",
    "inject-test-addon": "node scripts/inject-test-addon.js",
    "start-react-app": "npm start --prefix example-app",
    "publish:versions": "lerna version -m 'chore: bump versions' --no-push --conventional-commits",
    "publish:prerelease-versions": "lerna version -m 'chore: bump versions (prerelease)' --no-push --conventional-commits --conventional-prerelease --preid next",
    "test": "lerna run test -- -- --passWithNoTests",
    "lint-example-app": "eslint --ignore-pattern example-app/build example-app/",
    "test-example-app": "CI=true npm test --prefix example-app -- --passWithNoTests",
    "validate-example-app": "npm run lint-example-app && npm run test-example-app && prettier --check example-app/**/*.{ts,tsx} && npm run build --prefix example-app"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-lerna-changelog"
    }
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "license": "Apache-2.0",
  "author": "Amplicode team"
}
