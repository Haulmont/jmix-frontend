This folder contains custom templates used with stock generators. Templates should be put into separate folders, folder name should correspond to generator name, e.g.:

```
templates
├── app
│   ├── index.tsx.ejs
│   └── package.json
└── entity-management
    └── Table.tsx.ejs
```